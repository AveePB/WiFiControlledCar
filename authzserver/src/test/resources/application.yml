spring:
  datasource:
    url: jdbc:h2:mem:authz_test_db
    username: sa
    password: password
    driver-class-name: org.hibernate.dialect.H2Dialect

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true